// functions is helpers 
//   sumPaymentTotal(type) - what is type being passed 
//   calculateTipPercentage(billAmt, tipAmt) - in the tittle 
//   appendTD - append check if the tr has been appended to the table 


it("should calculate the tip percentage", function(){
    expect(calculateTipPercent(100, 10)).toEqual(10);
    expect(calculateTipPercent(200, 100)).toEqual(20);
})


it("should sum the the values in array", function(){
    expect(sumPaymentTotal( ).toEqual())
})

